
@using Rash_Airlines.Models
@model IEnumerable<display_ticket_Result>
@{
    ViewBag.Title = "Show_Tickets";
}

@using (Html.BeginForm())
{
    <div action="#" class="form-row" @*method="post"*@ novalidate="novalidate">
        <div class="row">
            <div class="col-lg-12">
                <div class="row">
                    <div class="col-lg-3 col-md-3 col-sm-12 p-0">
                        @Html.TextBox("booking_id", null, new { @class = "form-control" })

                    </div>

                </div>
                <div class="col-lg-3 col-md-3 col-sm-12 p-0">
                    <input type="submit" value="Search" class="btn btn-danger wrn-btn" />
                    @*<button type="button" class="btn btn-danger wrn-btn">Search</button>*@
                </div>
            </div>
        </div>
    </div>
    


}